currentNumber = 0

function addNum() {
    // figure out which button the user clicked
    // multiply currentNumber * 10 + the digit they clicked
    // currNum = 0, they click 5, output = 5
    // currNum = 1, they click 1, output = 11
    // currNum = 23, they click 4, output 234
}